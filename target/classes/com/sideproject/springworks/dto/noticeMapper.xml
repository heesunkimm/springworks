<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="com.sideproject.springworks.dto.NoticeMapper">

<!-- 	
 	<select id="listNotice" parameterType="java.util.Map" resultType="NoticeDTO"> 
		select * from ( 
			select rownum rn, A.* from( 
				select * from spw_notice order by notice_regDate desc ) A ) 
		where rn between #{startRow} and #{endRow}
	</select> 

	공지사항 총 개수
	<select id="getNoticeCount" resultType="int">
		select count(*) from spw_notice;
	</select> 
-->
</mapper>